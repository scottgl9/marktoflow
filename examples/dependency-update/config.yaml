# Dependency Update Bundle Configuration
agent: opencode
fallback_agent: claude-code

timeout: 900
max_retries: 3

tools_dir: tools
inherit_global_tools: true

env:
  GIT_AUTHOR_NAME: "marktoflow"
  GIT_AUTHOR_EMAIL: "marktoflow@example.com"
