# GitHub Personal Access Token (for GitHub API access only)
# Generate at: https://github.com/settings/tokens
# Required scopes: repo (for PR access and comments)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: Slack notifications for critical issues
SLACK_BOT_TOKEN=xoxb-xxxxxxxxxxxx-xxxxxxxxxxxx-xxxxxxxxxxxxxxxxxxxxxxxx

# ============================================================
# COPILOT AUTHENTICATION
# ============================================================
# GitHub Copilot uses OAuth authentication (NOT API keys).
# The workflow automatically uses the CLI's stored OAuth token.
#
# To authenticate (one-time setup):
#   1. Run: copilot auth login
#   2. Follow browser OAuth flow
#   3. Token saved to ~/.copilot/
#
# No COPILOT_API_KEY needed!
# ============================================================

# Optional: Custom Copilot CLI path (if not in PATH)
# COPILOT_CLI_PATH=/custom/path/to/copilot

# Optional: External Copilot CLI server (for development)
# COPILOT_CLI_URL=localhost:4321

