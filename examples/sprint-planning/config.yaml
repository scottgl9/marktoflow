# Sprint Planning Bundle Configuration
agent: opencode
fallback_agent: claude-code

timeout: 600
max_retries: 3

tools_dir: tools
inherit_global_tools: true

env:
  JIRA_API_VERSION: "3"
  CONFLUENCE_API_VERSION: "v2"
