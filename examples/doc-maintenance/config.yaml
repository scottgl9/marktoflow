# Documentation Maintenance Workflow Configuration

# Agent configuration
agent: ollama
fallback_agent: opencode

# Execution settings
timeout: 600
max_retries: 3

# Tool settings
tools_dir: tools
inherit_global_tools: true

# Environment variables
env:
  # Set these if not in global .env
  # OLLAMA_HOST: http://localhost:11434
  # OLLAMA_MODEL: llama3
  MARKTOFLOW_LOG_LEVEL: INFO
