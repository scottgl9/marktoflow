# Core dependencies
pydantic>=2.0
pyyaml>=6.0
jinja2>=3.1
typer>=0.9
rich>=13.0
httpx>=0.25
aiofiles>=23.0

# Optional and development dependencies live in pyproject.toml extras.
# Example: pip install .[dev] or pip install .[openai]

# Development dependencies (kept here for convenience running tests)
pytest>=7.0
pytest-asyncio>=0.21
